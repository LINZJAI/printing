#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_tslint@5.20.1@tslint/bin/tslint" "$@"
  ret=$?
else 
  node  "$basedir/../_tslint@5.20.1@tslint/bin/tslint" "$@"
  ret=$?
fi
exit $ret
